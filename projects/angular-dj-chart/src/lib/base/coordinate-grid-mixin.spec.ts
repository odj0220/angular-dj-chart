import { CoordinateGridMixin } from './coordinate-grid-mixin';

describe('CoordinateGridMixin', () => {
  it('should create an instance', () => {
    expect(new CoordinateGridMixin()).toBeTruthy();
  });
});
